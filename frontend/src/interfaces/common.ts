/* eslint-disable camelcase */

export enum UIRuntimeEnvName {
    DEV = 'dev',
    TEST = 'test',
    PROD = 'prod',
}

export type GUID = string;

export type IsoDate = string;
export type IsoDateTime = string;
